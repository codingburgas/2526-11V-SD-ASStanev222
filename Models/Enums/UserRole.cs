namespace ProjectLMS.Models.Enums;

/// <summary>
/// Enum representing user roles in the LMS.
/// Used for role-based authorization throughout the application.
/// </summary>
public enum UserRole
{
    Student = 0,
    Teacher = 1,
    Admin = 2
}
